apiVersion: v1
kind: Service
metadata:
  name: lms
  namespace: openedx
  labels:
    app: lms
    component: openedx
spec:
  type: ClusterIP
  selector:
    app: lms
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
